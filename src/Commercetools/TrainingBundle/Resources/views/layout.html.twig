{% block head %}
<!DOCTYPE html>
    <html>
        <head>
            <title>{% block title %}  Awesome webapp by Commercetools {% endblock %}</title>

            {% block stylesheets %}
                <link rel="stylesheet" href="{{ asset('bundles/ctp/css/bootstrap.css') }}" />
                <link rel="stylesheet" href="{{ asset('bundles/ctp/css/style.css') }}" />
            {% endblock %}

            {% block javascripts %}
                <script src="{{ asset('bundles/ctp/js/jquiry-2.3.3.js') }}"></script>
                <script src="{{ asset('bundles/ctp/js/index.js') }}"></script>
            {% endblock %}
        </head>
{% endblock %}

{% block menu %}
    <div class="menu">
       <a href="{{ path('_ctp_training_index') }}">
            <img class="menu-logo" src="{{ asset('bundles/ctp/img/CT-logo.svg') }}" alt="training"/>
       </a>
        <ul>
            <li><a href="{{ path('_ctp_training_index') }}">Home</a> </li>
            <li><a href="{{ path('_ctp_training_cart') }}">Cart</a> </li>
            {% if app.user %}
                <li><a href="{{ path('training_logout') }}">logout</a> </li>
            {% endif %}
            {% if not app.user %}
                <li><a href="{{ path('training_login') }}">login</a> </li>
            {% endif %}
        </ul>
    </div>

{% endblock %}

{% block body %}
    <body>
        <div class="content">
            {% block container %}
            {% endblock %}
        </div>
    </body>
{% endblock %}


